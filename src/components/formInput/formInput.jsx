/* eslint-disable no-unused-vars */
import React from 'react';

const FormInput = ({
  labelText, placeholder, row, contextName,
}) => {
  const test = 0;
  return (
    <div>formInput</div>
  );
};

export default FormInput;
